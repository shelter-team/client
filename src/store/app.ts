// import Vue from 'vue'

const defaultState = () => ({
	appId: null
})

const state = defaultState()

const actions = {}

const mutations = {}

export default {
	namespaced: true,
  state,
  mutations,
  actions,
}